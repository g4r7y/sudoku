# sudoku

Sudoku generator and solver.
For all your command line sudoku needs!

## Build

```make```

the binary will be output to the ```bin``` folder.

## Run

Generate a sudoku:

```sudoku generate```

Solve a sudoku:

```sudoku solve <sudoku-grid>```

For more usage information:

```sudoku help```
